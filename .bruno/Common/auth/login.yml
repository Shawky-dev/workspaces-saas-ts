info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/auth/common/login"
  body:
    type: json
    data: |-
      {
        "adminEmail":"1@1.com",
        "adminPassword":"123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
