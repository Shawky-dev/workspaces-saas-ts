info:
  name: get-tenant-by-slug
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/tenants/tenant-1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
