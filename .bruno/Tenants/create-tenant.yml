info:
  name: create-tenant
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/tenants"
  body:
    type: json
    data: |-
      {
        "tenantId": "tenant_1",
        "slug": "tenant-2",
        "name": "Tenant two",
        "mongoUri": "mongodb://localhost:27017/tenant2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
