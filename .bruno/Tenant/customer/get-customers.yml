info:
  name: get-customers
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/customers"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5