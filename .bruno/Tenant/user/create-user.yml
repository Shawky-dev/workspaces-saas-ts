info:
  name: create-user
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/{{tenantId}}/users"
  body:
    type: json
    data: |-
      {
        "name":"admin",
        "email":"1@1.com",
        "password":"password"
      }
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
