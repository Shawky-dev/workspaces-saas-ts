info:
  name: get-user
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/users"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
