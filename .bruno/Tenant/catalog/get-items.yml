info:
  name: get-items
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/catalog/items"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
