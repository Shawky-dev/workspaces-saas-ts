info:
  name: get-items
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/catalog/items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
