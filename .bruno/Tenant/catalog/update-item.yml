info:
  name: update-item
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{URL}}/{{tenantId}}/catalog/items/{{catalogItemId}}"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
