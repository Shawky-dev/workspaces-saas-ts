info:
  name: get-item
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/catalog/items/{{catalogItemId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
