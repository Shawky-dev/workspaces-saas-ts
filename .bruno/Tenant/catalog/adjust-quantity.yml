info:
  name: adjust-quantity
  type: http
  seq: 8

http:
  method: POST
  url: "{{URL}}/{{tenantId}}/catalog/items/{{catalogItemId}}/adjust-quantity"
  body:
    type: json
    data: |-
      {
        "delta": -2
      }
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
