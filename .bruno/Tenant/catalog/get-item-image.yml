info:
  name: get-item-image
  type: http
  seq: 6

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/catalog/items/{{catalogItemId}}/image"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
