info:
  name: delete-item
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{URL}}/{{tenantId}}/catalog/items/{{catalogItemId}}"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
