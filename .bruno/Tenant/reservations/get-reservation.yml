info:
  name: get-reservation
  type: http
  seq: 6

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/reservations/{{reservationId}}"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
