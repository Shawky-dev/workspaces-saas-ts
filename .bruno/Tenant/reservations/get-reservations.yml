info:
  name: get-reservations
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/reservations?weekStart={{weekStart}}"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
