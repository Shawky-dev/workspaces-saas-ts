info:
  name: get-reservation-timetable
  type: http
  seq: 4

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/reservations/timetable?weekStart={{weekStart}}"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
