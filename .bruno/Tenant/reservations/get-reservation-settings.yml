info:
  name: get-reservation-settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/reservation-settings"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
