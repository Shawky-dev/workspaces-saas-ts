info:
  name: confirm-arrival
  type: http
  seq: 9

http:
  method: POST
  url: "{{URL}}/{{tenantId}}/reservations/{{reservationId}}/confirm-arrival"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
