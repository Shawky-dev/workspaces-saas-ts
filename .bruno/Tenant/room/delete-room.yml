info:
  name: delete-room
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{URL}}/{{tenantId}}/rooms/{{roomId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5