info:
  name: get-rooms
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/rooms"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5