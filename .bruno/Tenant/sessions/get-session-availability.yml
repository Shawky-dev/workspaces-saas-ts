info:
  name: get-session-availability
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/sessions/availability"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
