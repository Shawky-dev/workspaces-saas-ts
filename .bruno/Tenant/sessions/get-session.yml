info:
  name: get-session
  type: http
  seq: 4

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/sessions/{{sessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
