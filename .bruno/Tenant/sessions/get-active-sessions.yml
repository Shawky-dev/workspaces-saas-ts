info:
  name: get-active-sessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/sessions/active"
  auth:
    type: bearer
    token: "{{JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
