info:
  name: get-receipts
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/receipts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
