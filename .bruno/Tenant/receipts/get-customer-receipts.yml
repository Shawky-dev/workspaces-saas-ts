info:
  name: get-customer-receipts
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/customers/{{customerId}}/receipts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
