info:
  name: get-receipt
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/{{tenantId}}/receipts/{{receiptId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
